
---

# 🧩 TECHNICAL_REQUIREMENTS.md

*Проект: ETL-платформа для аналитических витрин онлайн-кинотеатра*

---

## 1. Функциональные требования

### 1.1. Источники и приёмники

1. Система должна уметь читать данные из существующих БД онлайн-кинотеатра:

   * `content` (как минимум — таблица `film_work`);
   * `ugc` (как минимум — таблица `ratings`).
2. Система должна уметь записывать данные в аналитическую схему `analytics`:

   * `analytics.film_dim`;
   * `analytics.film_rating_agg`.
3. Добавление новых витрин должно происходить **через конфигурацию пайплайнов**, без изменения общей ETL-логики:

   * описанием новых пайплайнов в таблицах `etl_pipelines` / `etl_pipeline_tasks`;
   * без правки кода ядра Runner’а.

---

### 1.2. Конфигурация пайплайнов и задач

1. Каждый ETL-пайплайн описывается записью в таблице `etl.etl_pipelines`.
2. Внутри пайплайна логика может быть разбита на **один или несколько шагов** (tasks), описанных в `etl.etl_pipeline_tasks`.
3. Поддерживаются два типа задач:

   * `SQL` — SQL-трансформации;
   * `PYTHON` — вызов заранее зарегистрированных Python-тасков.
4. Для SQL-пайплайнов минимальная конфигурация включает:

   * `source_query` — текст SQL-запроса или базовый запрос-источник;
   * `target_table` — имя таблицы-приёмника в `analytics`;
   * `mode` — режим работы: `full` или `incremental`;
   * `batch_size` — размер батча;
   * (опц.) `incremental_key` — поле для инкрементального отбора (например, `modified`, `id`).
5. Для Python-задач:

   * в конфигурации хранится `python_task` / `task_name` — имя заранее зарегистрированной задачи в коде;
   * исполнение управляется тем же Runner’ом, без отдельного оркестратора.
6. Конфигурация пайплайнов должна уметь изменяться **через API**:

   * изменение `source_query`, `batch_size`, `enabled`, `mode`, `target_table` (в рамках списка разрешённых);
   * при статусе `RUNNING` должны действовать ограничения:

     * изменения части полей запрещены (например, `target_table`, `mode`, `incremental_key`);
     * для критических изменений может быть требование предварительно перевести пайплайн в `PAUSED` или `IDLE`.

---

### 1.3. API управления пайплайнами (ETL API)

1. Должен быть реализован HTTP API (FastAPI) для управления пайплайнами, который предоставляет как минимум:

   * получение списка пайплайнов;
   * просмотр деталей конкретного пайплайна;
   * создание нового пайплайна (MVP — SQL-пайплайны);
   * обновление конфигурации существующего пайплайна;
   * включение/отключение пайплайна (`enabled`);
   * запрос на запуск пайплайна (`run`);
   * запрос на паузу пайплайна (`pause`).
2. API должен предоставлять эндпоинты для получения:

   * текущего статуса пайплайна (`IDLE / RUNNING / PAUSED / FAILED`);
   * текущего состояния (`etl_state` — checkpoint);
   * списка последних запусков (`etl_runs` с базовой информацией).
3. API должен иметь технический эндпоинт `healthcheck` для проверки работоспособности (например, `/health`).

---

### 1.4. Исполнение пайплайнов (ETL Runner)

1. Runner должен периодически опрашивать таблицу `etl_pipelines` и:

   * запускать пайплайны со статусом `RUN_REQUESTED`;
   * ставить на паузу пайплайны со статусом `PAUSE_REQUESTED` (после завершения текущего батча).
2. Runner должен работать только с пайплайнами, у которых `enabled = TRUE`.
3. Для каждого пайплайна Runner обязан:

   * прочитать текущее состояние из `etl_state`;
   * сформировать запрос/выборку из источника:

     * с учётом режима (`full` / `incremental`);
     * с учётом `last_processed_*` в `etl_state`;
   * забирать данные батчами (`batch_size`);
   * применить трансформацию:

     * минимум — маппинг полей и простые преобразования;
     * в перспективе — цепочку задач (SQL/Python) из `etl_pipeline_tasks`;
   * записывать данные в целевую таблицу:

     * с UPSERT-семантикой по ключу (например, `id`);
   * обновлять:

     * `etl_state` (checkpoint);
     * `etl_runs` (статус, количество строк, тайминги).
4. Runner должен поддерживать **многошаговый пайплайн**, но в рамках MVP допускается реализация с одним task на пайплайн (архитектура при этом должна позволять расширение).

---

### 1.5. Режимы работы: full и incremental

1. **Full-режим (`mode = 'full'`)**:

   * при запуске перечитывает все данные из источника;
   * допускается реализация:

     * через `TRUNCATE + INSERT` в целевую таблицу;
     * или через массовый UPSERT поверх существующих данных;
   * должен корректно отрабатывать повторный запуск.
2. **Инкрементальный режим (`mode = 'incremental'`)**:

   * выбирает только новые/изменённые записи на основе:

     * `incremental_key` (например, `modified` или `id`);
     * значений checkpoint’а в `etl_state`;
   * повторный запуск не должен приводить к дублированию данных (идемпотентность).
3. Реализация incremental может быть выполнена во 2–3 неделю, при этом архитектурная поддержка (`mode`, `incremental_key`, `etl_state`) должна быть заложена сразу.

---

### 1.6. Ошибки, статусы и отчётность

1. При каждой попытке запуска пайплайна должна создаваться запись в `etl_runs`:

   * `started_at`, `finished_at`;
   * `rows_read`, `rows_written`;
   * статус запуска (`SUCCESS` / `FAILED`).
2. В случае ошибки:

   * статус конкретного запуска в `etl_runs` должен быть `FAILED`;
   * сохраняется текст ошибки (усечённый до разумной длины — например, до N символов);
   * статус пайплайна в `etl_pipelines` переводится в `FAILED` (до ручного вмешательства).
3. Через API должно быть возможно:

   * получить список недавних запусков пайплайна;
   * просмотреть статус и агрегированные метрики (кол-во строк, ошибка, длительность).

---

### 1.7. Reconcile и дополнительные операции

1. Система должна предусматривать возможность **reconcile** (сравнение source/target) для пайплайна, как минимум:

   * простая проверка количества строк;
   * базовая сверка по контрольному полю (например, максимум/минимум `id` или `modified`).
2. В MVP допускается реализация reconcile в виде:

   * отдельного эндпоинта,
   * или утилитарной команды/скрипта, работающего поверх тех же конфигов.

---

## 2. Нефункциональные требования

### 2.1. Идемпотентность и надёжность

1. Запись данных в `analytics` должна быть **идемпотентной**:

   * повторный запуск того же батча не должен создавать дубли;
   * основой является UPSERT по ключу (например, `id` или составному ключу).
2. Состояние пайплайна (`etl_state`) обновляется **только после** успешного завершения батча:

   * при ошибке checkpoint остаётся на предыдущем значении;
   * это гарантирует возможность безопасного повторного запуска.
3. При падении Runner’а:

   * следующий запуск должен продолжить работу с того места, где остановился (по `etl_state`);
   * не должно быть критичных side-effect’ов в `analytics` (благодаря UPSERT и правилу обновления state).

---

### 2.2. Производительность и нагрузка

1. Чтение из источников (`content`, `ugc`) должно быть **батчевым**:

   * использование `LIMIT/OFFSET` или отбора по ключу;
   * размер батча управляется параметром `batch_size`.
2. ETL не должен создавать длительные блокировки на рабочих таблицах:

   * чтение — через read-only транзакции;
   * запись в `analytics` — по возможности в отдельной схеме/таблицах, не влияющих на OLTP-запросы.
3. Время выполнения одного батча должно быть **разумным**:

   * ориентир для тестового стенда — < 300 мс для простых витрин;
   * требование мягкое, цель — избежать слишком больших транзакций и блокировок.
4. Система должна позволять настройку `batch_size` и частоты запусков таким образом, чтобы нагрузка могла быть адаптирована под продовый контур (в будущем).

---

### 2.3. Безопасность

1. ETL API не должен позволять выполнять произвольный SQL в произвольные таблицы:

   * `target_table` должен быть ограничен схемой `analytics` или whitelist’ом допустимых таблиц;
   * `source_query` должен ограничиваться чтением из `content` / `ugc` (без `UPDATE/DELETE` и т.п.).
2. Для доступа к БД должна использоваться выделенная учётная запись:

   * read-доступ к схемам `content`, `ugc`;
   * write-доступ только к `analytics` и `etl`.
3. Конфигурация подключения к БД (URL, логин, пароль) должна храниться:

   * в переменных окружения / .env-файлах;
   * не должна попадать в Git-репозиторий.
4. В режиме `PYTHON`:

   * Runner не выполняет произвольный код, переданный через БД;
   * `python_task` должен ссылаться на заранее зарегистрированную функцию/класс в коде;
   * API не может загружать и исполнять произвольные `.py` файлы от пользователя.

---

### 2.4. Обслуживание и наблюдаемость

1. Все ключевые действия ETL должны логироваться:

   * запуск пайплайна;
   * завершение батча (успех/ошибка);
   * переходы статусов (`RUNNING`, `PAUSED`, `FAILED`);
   * ошибки соединения, ошибки SQL.
2. Логи должны содержать:

   * имя пайплайна и его `id`;
   * `etl_runs.id` (идентификатор запуска);
   * количество прочитанных/записанных строк;
   * ключевые тайминги (время выполнения батча, общее время запуска).
3. Структура кода должна быть модульной:

   * отдельные модули/пакеты для:

     * API;
     * Runner’а;
     * работы с БД (репозитории);
     * конфигурации и моделей;
     * вспомогательных утилит.
4. Желательно предусмотреть базовую интеграцию с системами мониторинга (метрики по числу успешных/проваленных запусков, времени исполнения).

---

### 2.5. Развёртывание

1. Проект должен подниматься полностью через `docker compose`:

   * Postgres;
   * ETL API;
   * ETL Runner.
2. Инициализация БД должна выполняться через миграции:

   * создание схем и таблиц для `analytics` и `etl`;
   * опционально — наполнение тестовыми данными и начальными пайплайнами.
3. Для локального запуска должны быть доступны удобные команды (Makefile или скрипты), например:

   * `make up` — поднять весь стенд;
   * `make down` — остановить и очистить;
   * `make migrate-db` — применить миграции;
   * `make logs` — просмотр логов основных сервисов.
4. Архитектура должна позволять в будущем:

   * вынести Runner под внешний оркестратор (Airflow) без переписывания ядра;
   * масштабировать Runner горизонтально (несколько экземпляров при координации по state).

---
